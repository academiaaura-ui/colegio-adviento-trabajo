const menuBtn=document.getElementById('menuBtn');const nav=document.getElementById('navLinks');menuBtn.addEventListener('click',()=>nav.classList.toggle('open'));document.querySelectorAll('.chip').forEach(chip=>chip.addEventListener('click',()=>{document.querySelectorAll('.chip').forEach(c=>c.classList.remove('active'));chip.classList.add('active');const f=chip.dataset.filter;document.querySelectorAll('.resource').forEach(r=>r.classList.toggle('hidden',f!=='all'&&r.dataset.grade!==f));}));document.querySelectorAll('[data-grade]').forEach(a=>a.addEventListener('click',()=>{setTimeout(()=>{const c=document.querySelector('.chip[data-filter="'+a.dataset.grade+'"]');if(c)c.click();},120);}));document.getElementById('academicForm').addEventListener('submit',e=>{e.preventDefault();document.getElementById('formMessage').textContent='La plataforma está lista. El siguiente paso es conectar este formulario con Google Forms.';});